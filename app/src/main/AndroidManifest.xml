<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mattheus.trabalho_01_dcc196">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.CadastrarParticipanteActivity" />
        <activity android:name=".Activitys.CadastrarEventoActivity" />
        <activity android:name=".Activitys.ListarParticipantesActivity" />
        <activity android:name=".Activitys.EditarParticipante" />
        <activity android:name=".Activitys.EditarEvento"></activity>
    </application>

</manifest>